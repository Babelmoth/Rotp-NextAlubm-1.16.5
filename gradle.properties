# Sets default memory used for gradle commands. Can be overridden by user or command line properties.
# This is required to provide enough memory for the Minecraft decompilation process.
org.gradle.jvmargs=-Xmx3G
org.gradle.daemon=false
# Gradle 使用的 JDK 8 路径（无 JAVA_HOME 时靠此构建）。若报 invalid，请改为本机实际 JDK 8 目录。
# Use forward slashes and ensure the path points to a JDK (not JRE) installation
# Note: Path with spaces may need to be quoted or use different format
org.gradle.java.home=C:/Program Files/Eclipse Adoptium/jdk-8.0.472.8-hotspot

addon_version=0.1
# https://github.com/StandoByte/RotP-maven/commits/main
main_mod_version=1.16.5-0.2.2-snapshot-250430-a

playeranim_version = 0.4.0+1.16.5
bendylib_version = 1.2.2
systemProp.net.minecraftforge.gradle.check.certs=false